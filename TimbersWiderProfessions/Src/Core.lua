-- Creates the addon frame every other file attaches to. Loaded first on every
-- client; the Classic window (main.lua) and the Forever window both hang off it.
CreateFrame("Frame", "TimbersWiderProfessionsAddon")
